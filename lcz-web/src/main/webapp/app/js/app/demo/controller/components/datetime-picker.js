"use strict";
define(["main-app", "jquery", "tools"
], function (app, $, tools) {
    app.controller("DatetimePickerController", ["$scope", "$timeout", "http",
        function ($scope, $timeout, http) {

        }
    ]);
});